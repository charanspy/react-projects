Run pnpm install (or npm install):->This reads your package.json file and downloads all required dependencies into a fresh node_modules folder on that machine.
Before running : npm run dev or pnpm dev

This is Profile build CHALLENGE-2
